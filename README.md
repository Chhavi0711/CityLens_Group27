# CityLens_Group27
Resources for CityLens AI Hackathon 2026, Group 27 (Topic -Traffic Violations &amp; Enforcement)

# 1. Clone / create your repo and add these files
# 2. Install everything
pip install -r requirements.txt

# 3. Get your Roboflow API key at https://app.roboflow.com → Settings → API
export ROBOFLOW_API_KEY="your_key_here"

# 4. Download all datasets
python download_data.py --task all
